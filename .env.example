# Turso Database Configuration
# Get these values from Vercel Dashboard > Storage > Turso
TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Application Version
# Displayed in the bottom-right corner of all pages
APP_VERSION=v1.2.0

# Admin Authentication Token
# Temporary token used until JWT authentication is implemented (Phase 1.5)
# Generate a secure random string (32+ characters)
# Example: openssl rand -hex 32
ADMIN_AUTH_TOKEN=your_secure_admin_token_here

# JWT Secret (for Phase 1.5)
# Used to sign and verify JWT tokens
# Generate a secure random string (32+ characters)
# Example: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here
