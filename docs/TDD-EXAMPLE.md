# Практический пример TDD: Добавление "Сухой победы мафии"

## 🎯 Задача

Добавить правильный подсчёт "**Победа в сухую**" для мафии:
- Мафия победила
- Все мафы остались живы

Сейчас в коде есть логика, но она может быть неправильной. Проверим через TDD!

---

## Шаг 1: 🔴 RED - Пишем тест

### Создаём тестовый файл

```bash
touch __tests__/dry-win.test.js
```

### Пишем тест

```javascript
// __tests__/dry-win.test.js
import { describe, test, expect } from '@jest/globals';

// Импортируем функцию, которую будем тестировать
import { analyzeGame } from '../rating_calculator.js';

describe('Dry Win (Победа в сухую) для Мафии', () => {

  test('Должна быть победа в сухую если вся мафия жива', () => {
    const players = [
      { name: 'C1', role: 'Мирный', killed_when: '1N' },
      { name: 'C2', role: 'Мирный', killed_when: '2N' },
      { name: 'C3', role: 'Мирный', killed_when: '3N' },
      { name: 'C4', role: 'Мирный', killed_when: '3D' },
      { name: 'C5', role: 'Мирный', killed_when: '3D' },
      { name: 'S', role: 'Шериф', killed_when: '2N' },
      { name: 'M1', role: 'Мафия', killed_when: '0' },   // ЖИВ
      { name: 'M2', role: 'Мафия', killed_when: '0' },   // ЖИВ
      { name: 'D', role: 'Дон', killed_when: '0' }       // ЖИВ
    ];

    const result = analyzeGame(players);

    expect(result.winner).toBe('Мафия');
    expect(result.is_dry_win).toBe(true);  // ⬅️ ЭТО ГЛАВНОЕ!
  });

  test('НЕ должна быть победа в сухую если хоть одна мафия убита', () => {
    const players = [
      { name: 'C1', role: 'Мирный', killed_when: '1N' },
      { name: 'C2', role: 'Мирный', killed_when: '2N' },
      { name: 'C3', role: 'Мирный', killed_when: '3N' },
      { name: 'C4', role: 'Мирный', killed_when: '3D' },
      { name: 'C5', role: 'Мирный', killed_when: '3D' },
      { name: 'S', role: 'Шериф', killed_when: '2N' },
      { name: 'M1', role: 'Мафия', killed_when: '2D' },  // УБИТ!
      { name: 'M2', role: 'Мафия', killed_when: '0' },   // ЖИВ
      { name: 'D', role: 'Дон', killed_when: '0' }       // ЖИВ
    ];

    const result = analyzeGame(players);

    expect(result.winner).toBe('Мафия');
    expect(result.is_dry_win).toBe(false);  // ⬅️ НЕТ бонуса!
  });

  test('НЕ должна быть победа в сухую если победили мирные', () => {
    const players = [
      { name: 'C1', role: 'Мирный', killed_when: '0' },
      { name: 'C2', role: 'Мирный', killed_when: '0' },
      { name: 'C3', role: 'Мирный', killed_when: '0' },
      { name: 'S', role: 'Шериф', killed_when: '0' },
      { name: 'M1', role: 'Мафия', killed_when: '1D' },
      { name: 'M2', role: 'Мафия', killed_when: '2D' },
      { name: 'D', role: 'Дон', killed_when: '3D' }
    ];

    const result = analyzeGame(players);

    expect(result.winner).toBe('Мирные');
    expect(result.is_dry_win).toBe(false);
  });

  test('Мафия получает +1 очко за победу в сухую', () => {
    const players = [
      { name: 'C1', role: 'Мирный', killed_when: '1N' },
      { name: 'C2', role: 'Мирный', killed_when: '2N' },
      { name: 'C3', role: 'Мирный', killed_when: '3N' },
      { name: 'C4', role: 'Мирный', killed_when: '3D' },
      { name: 'C5', role: 'Мирный', killed_when: '3D' },
      { name: 'S', role: 'Шериф', killed_when: '2N' },
      { name: 'M1', role: 'Мафия', killed_when: '0' },   // ЖИВ
      { name: 'M2', role: 'Мафия', killed_when: '0' },   // ЖИВ
      { name: 'D', role: 'Дон', killed_when: '0' }       // ЖИВ
    ];

    const result = global.calculateGame(players, '');

    // Мафия: 5 (победа) + 1 (в сухую) = 6
    const mafia = result.results.find(r => r.role === 'Мафия');
    expect(mafia.points).toBe(6);
    expect(mafia.achievements).toContain('Победа в сухую');
  });
});
```

---

## Шаг 2: Запускаем тест

```bash
npm run tdd:file -- dry-win.test.js
```

### Результат: 🔴 RED

```
FAIL __tests__/dry-win.test.js
  ● Dry Win (Победа в сухую) для Мафии › Должна быть победа в сухую если вся мафия жива

    TypeError: analyzeGame is not exported from rating_calculator.js

    Expected: true
    Received: undefined
```

**Отлично!** Тест падает, как и ожидалось. Теперь можно писать код.

---

## Шаг 3: 🟢 GREEN - Минимальный код

### Экспортируем функцию

```javascript
// rating_calculator.js

// Добавляем export
export function analyzeGame(players) {
  // ... существующий код ...

  // Победа мафии в сухую
  const is_dry_win = winner === 'Мафия' &&
    mafiaPlayers.every(p => p.killed_when === '0' || !p.killed_when);

  return {
    winner,
    is_clean_win,
    is_dry_win,  // ⬅️ Возвращаем флаг
    is_guessing,
    guessing_players
  };
}
```

### Запускаем тест снова

```bash
npm run tdd:file -- dry-win.test.js
```

### Результат: 🟢 GREEN

```
PASS __tests__/dry-win.test.js
  Dry Win (Победа в сухую) для Мафии
    ✓ Должна быть победа в сухую если вся мафия жива (5ms)
    ✓ НЕ должна быть победа в сухую если хоть одна мафия убита (2ms)
    ✓ НЕ должна быть победа в сухую если победили мирные (1ms)
    ✓ Мафия получает +1 очко за победу в сухую (3ms)

Test Suites: 1 passed, 1 total
Tests:       4 passed, 4 total
```

**✅ Все тесты проходят!**

---

## Шаг 4: 🔵 REFACTOR - Улучшаем код

### Проблемы текущего кода:

1. Дублирование логики "игрок жив"
2. Магические строки `'0'`
3. Нет констант

### Улучшенная версия:

```javascript
// rating_calculator.js

// Константы
const ALIVE_STATUS = '0';

// Вспомогательные функции
function isPlayerAlive(player) {
  return player.killed_when === ALIVE_STATUS || !player.killed_when;
}

export function analyzeGame(players) {
  const alivePlayers = players.filter(isPlayerAlive);

  // Определяем победителя
  const aliveMafia = alivePlayers.filter(p =>
    p.role === 'Мафия' || p.role === 'Дон'
  ).length;

  const aliveCivilians = alivePlayers.filter(p =>
    p.role === 'Мирный' || p.role === 'Шериф'
  ).length;

  const winner = aliveMafia === 0 ? 'Мирные' : 'Мафия';

  // Чистая победа мирных
  const civilianPlayers = players.filter(p =>
    p.role === 'Мирный' || p.role === 'Шериф'
  );

  const noCivilianKilledByVote = !civilianPlayers.some(p =>
    p.killed_when && p.killed_when.includes('D')
  );

  const is_clean_win = winner === 'Мирные' && noCivilianKilledByVote;

  // Победа мафии в сухую
  const mafiaPlayers = players.filter(p =>
    p.role === 'Мафия' || p.role === 'Дон'
  );

  const allMafiaAlive = mafiaPlayers.every(isPlayerAlive);
  const is_dry_win = winner === 'Мафия' && allMafiaAlive;

  // ... остальной код ...

  return {
    winner,
    is_clean_win,
    is_dry_win,
    is_guessing,
    guessing_players
  };
}
```

### Запускаем все тесты

```bash
npm test
```

### Результат: ✅ ВСЕ ТЕСТЫ ПРОХОДЯТ

```
Test Suites: 12 passed, 12 total
Tests:       170 passed, 170 total
```

**🎉 Готово!** Код стал чище, а функциональность работает правильно!

---

## Шаг 5: Коммит

```bash
git add __tests__/dry-win.test.js rating_calculator.js
git commit -m "feat: add dry win tests and improve code quality

- Add comprehensive tests for dry win logic
- Extract isPlayerAlive helper function
- Add ALIVE_STATUS constant
- Improve code readability

TDD approach: Red → Green → Refactor"
```

---

## 📊 Статистика

**До TDD:**
- Код работал, но непонятно правильно ли
- Нет уверенности в логике
- Страшно менять

**После TDD:**
- 4 теста покрывают все сценарии
- 100% уверенность в работе
- Можно рефакторить без страха
- Регрессия невозможна

---

## 🎓 Уроки из этого примера

### 1. Тесты как спецификация

Тесты явно описывают **ЧТО** должна делать функция:
- "Мафия в сухую = вся мафия жива"
- "Не в сухую = хоть одна мафия убита"

### 2. Тестируй граничные случаи

```javascript
test('Победа мирных НЕ считается сухой для мафии', () => {
  // Даже если вся мафия жива, но они проиграли
});
```

### 3. Тестируй интеграцию

```javascript
test('Мафия получает +1 очко за победу в сухую', () => {
  // Проверяем не только флаг, но и начисление очков
});
```

### 4. Refactoring безопасен

После рефакторинга все тесты прошли → код работает правильно!

---

## 🚀 Следующие шаги

Теперь, когда ты увидел TDD в действии, попробуй:

1. **Написать тест для нового достижения**
   - Например: "Снайпер" (все проверки шерифа были чёрными)

2. **Найти баг через тест**
   - Напиши тест, который падает
   - Исправь код
   - Тест должен пройти

3. **Покрыть тестами существующий код**
   - Начни с `rating_calculator.js`
   - Добавь тесты для всех функций

---

## ✅ Итого

**TDD** — это не просто написание тестов. Это:
- 🎯 **Проектирование** через тесты
- 🛡️ **Защита** от регрессии
- 📝 **Документация** кода
- 💪 **Уверенность** в изменениях

**Начни применять TDD прямо сейчас!** 🚀
